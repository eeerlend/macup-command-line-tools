# macup-command-line-tools

Installs xcode command line tools

## Installation
Run the following command to add it to your repo

```bash
npm install eeerlend/macup-command-line-tools --save
```

## Configuration

No configuration
